# OWKR EXAM 디자인 가이드

## 1. 디자인 방향

OWKR EXAM은 홍보용 랜딩 페이지가 아니라, 시험 응시와 운영을 위한 도구입니다. 디자인의 목표는
화려함보다 **명확성, 신뢰감, 빠른 작업 흐름**입니다.

- 응시 화면: 담백한 한국형 CBT 시험지
- 관리자 화면: 표·목록·상세 중심의 운영 도구
- 시각 언어: 밝은 종이색, 진한 본문, OWKR 블루 한 가지 강조색
- 움직임: 상태를 설명하는 최소한의 전환만 사용하고 장식성 애니메이션은 사용하지 않습니다.

`Toss`, `Apple`, `Vercel`의 절제된 정보 위계는 참고할 수 있지만, 특정 서비스의 UI를 그대로
모방하지 않습니다. 모든 화면은 시험 시스템의 목적에 맞춰 결정합니다.

## 2. 기본 토큰

| 용도        | 값             | 사용 기준                    |
| ----------- | -------------- | ---------------------------- |
| 본문/강조선 | `#111820`      | 제목, 본문, 시험지 구분선    |
| 보조 텍스트 | `#6A7684`      | 도움말, 날짜, 부가 정보      |
| 배경        | `#F7F8FA`      | 전체 페이지 바탕             |
| 표면        | `#FFFFFF`      | 시험지, 표, 입력 폼          |
| 일반 테두리 | `#DFE4E9`      | 카드, 입력창, 행 구분        |
| 강한 구분선 | `#C8D0D9`      | 표 머리글, 주요 영역 구분    |
| OWKR 블루   | `#087BA8`      | 주요 행동, 현재 상태, 링크   |
| 옅은 블루   | `#EAF7FB`      | 선택됨, 답변 완료, 안내 배경 |
| 위험        | `#B42318` 계열 | 삭제, 복구 불가 행동         |

블루는 한 화면의 핵심 행동과 상태 표시를 위해서만 사용합니다. 같은 화면에 여러 강조색을
경쟁시키지 않습니다.

## 3. 타이포그래피와 간격

- 기본 글꼴: `Pretendard`, `Noto Sans KR`, `Apple SD Gothic Neo` 순서의 시스템 폴백
- 제목: 굵기 700, 자간을 약간 좁게 사용
- 본문: 14–16px, 충분한 줄간격 유지
- 도움말/메타 정보: 12px 전후, 보조 텍스트 색 사용
- 숫자, 시간, 코드: `font-mono`와 tabular number를 우선 사용
- 본문 줄간격: 긴 문제·답안은 `leading-6` 또는 `leading-7` 이상
- 기본 여백 단위: 4px 기반. 섹션 내부는 16/20/24px, 화면 단위는 32px 이상을 우선합니다.

긴 한국어 문장은 중간에 임의로 잘라내지 않습니다. 목록에서 한 줄 요약이 필요할 때만
`truncate`를 사용하고, 전체 내용은 상세 화면 또는 `title`로 확인할 수 있어야 합니다.

## 4. 공통 컴포넌트

### 버튼

- `btn`: 등록, 저장, 제출처럼 화면당 가장 중요한 행동 한 가지
- `btn-secondary`: 이전, 취소, 보조 저장처럼 되돌릴 수 있는 행동
- 위험 행동: 빨간색 외곽 또는 옅은 빨간 배경을 사용하고, 주요 파란 버튼과 같은 그룹에 두지 않습니다.
- 링크형 버튼은 목록 탐색이나 가벼운 보조 행동에만 사용합니다.
- 버튼 높이는 같은 작업 그룹 안에서 통일합니다.

### 입력창

- 항상 보이는 `label`을 사용합니다. placeholder는 설명의 대체 수단이 아닙니다.
- 포커스에는 OWKR 블루 테두리와 옅은 블루 ring을 사용합니다.
- 오류는 입력 근처에 한국어로 설명하며 색상만으로 상태를 전달하지 않습니다.

### 표와 목록

- 관리 목록은 필요한 열만 유지합니다. 한 행에서 모든 정보를 보여주려 하지 않습니다.
- 좁은 작업 영역에서는 `번호 · 제목 · 유형 · 배점`처럼 핵심 열만 표시하고, 나머지는 상세 패널로 보냅니다.
- 유형·상태·코드처럼 의미가 고정된 값은 줄바꿈하지 않습니다.
- 행을 클릭하면 상세를 열 수 있어야 하며, 상세에서 수정·삭제 같은 후속 행동을 제공합니다.

### 상태 배지

- `badge`를 사용합니다.
- 색상은 보조 수단입니다. 반드시 `응시 중`, `채점 필요`, `채점 완료`처럼 텍스트를 함께 표시합니다.
- 배지를 장식용으로 남발하지 않습니다.

### 알림과 확인창

- 완료·실패의 짧은 피드백: `svelte-sonner` Toast
- 삭제·제출처럼 되돌릴 수 없는 행동: shadcn-svelte `AlertDialog`
- 확인창은 가로 448px 이하를 기본으로 하며, 제목·설명·버튼만 남긴 컴팩트한 형태를 유지합니다.
- 확인창은 화면 중앙에 표시하고, 배경은 약한 어두운 오버레이와 아주 작은 blur만 적용합니다.
- 위험 행동의 확인 버튼은 빨간색, 취소 버튼은 테두리형으로 사용합니다.
- 브라우저 기본 `confirm()`은 사용하지 않습니다.

## 5. 응시 화면: 한국형 CBT

응시자는 문제를 푸는 데만 집중할 수 있어야 합니다.

- 헤더에는 `OWKR EXAM`과 남은 시간만 둡니다.
- 상단 시험 정보에는 시험명, 현재 문제, 전체 문항 수, 답변 완료 수를 표시합니다.
- 좌측 문항 패널에는 번호, 현재 문제, 답변 완료 상태, 키보드 단축키, 제출 행동을 둡니다.
- 우측 문제지는 흰 바탕과 강한 상단/하단 구분선으로 시험지 성격을 명확히 합니다.
- 객관식 선택지는 충분한 클릭 영역과 번호를 제공하고, 선택 상태를 분명하게 표시합니다.
- 이전/다음은 문제지 하단의 고정된 위치에 둡니다. 문제 길이에 따라 위치가 흔들리면 안 됩니다.
- 제출은 명확한 확인창을 거친 뒤 실행합니다.
- 점수, 정답, 채점 세부 내용은 응시자에게 노출하지 않는 정책을 UI에서도 지킵니다.

## 6. 관리자 화면: 운영 도구

### 레이아웃

- 데스크톱: 좌측 메뉴, 우측 작업 영역
- 모바일: 메뉴를 가로 스크롤로 전환
- 좌측 메뉴는 현재 섹션을 블루 선과 옅은 배경으로 표시합니다.
- 우측 작업 영역 상단에는 `시험 관리 > 섹션 > 상세` 순서의 Breadcrumb을 둡니다.
- 목록과 상세는 같은 화면에 중복 노출하지 않고, Breadcrumb으로 현재 위치와 복귀 경로를 알립니다.

### 문제 관리

- 문제 목록과 문제 상세는 각각 작업 영역 전체 너비를 사용합니다.
- 목록을 선택하면 현재 목록 데이터를 사용해 상세가 즉시 열리고, 목록은 숨깁니다.
- 목록 복귀는 Breadcrumb의 `문제 관리` 항목으로 통일합니다.
- 상세 상단의 작업은 `수정/취소`, `문제 삭제`로 구분합니다.
- 삭제는 우측 위험 행동으로 분리하며 확인창을 거칩니다.
- 수정 이력, 보기, 정답, 충돌 안내는 상세에만 둡니다.

### 응시 코드·결과

- 코드 발급 폼은 목록과 분리하되 같은 화면에서 빠르게 작업할 수 있게 둡니다.
- 결과 표는 지원자, 상태, 점수, 응시 시간 등 운영 판단에 필요한 정보부터 표시합니다.
- 채점 화면은 문제, 응시 답안, 배점, 직접 입력 점수가 차례로 읽히게 구성합니다.
- 채점 상세가 열리면 결과 목록은 숨기고 Breadcrumb으로 목록에 복귀합니다.

## 7. 반응형 원칙

- 데스크톱을 우선하되, 320px 이상에서 읽고 조작할 수 있어야 합니다.
- 표는 무리하게 모든 열을 압축하지 않습니다. 핵심 열만 남기거나 필요할 때 가로 스크롤을 제공합니다.
- 긴 문제와 답안은 수평 스크롤을 만들지 않고 세로로 자연스럽게 확장합니다.
- 모바일에서는 고정 사이드바를 일반 흐름으로 전환하고, 주요 버튼은 충분한 터치 영역을 유지합니다.

## 8. 접근성

- 모든 입력에는 label 또는 접근 가능한 이름을 제공합니다.
- 현재 문제, 답변 여부, 저장 실패, 제출 완료 같은 상태는 텍스트로도 전달합니다.
- 키보드만으로 문제 이동, 객관식 선택, 제출 취소를 할 수 있어야 합니다.
- 확인창은 열릴 때 포커스를 받고, Escape로 취소할 수 있으며, 배경 요소가 포커스를 받지 않아야 합니다.
- 색상 대비를 유지하고, 파란색·빨간색만으로 의미를 구분하지 않습니다.

## 9. 피해야 할 것

- 과도한 그라데이션, 글래스 효과, 큰 그림자, 장식 아이콘
- 한 화면에 여러 개의 주요 파란 버튼
- 카드 안에 또 카드를 넣는 과도한 계층
- 관리 목록에서 너무 많은 열을 보여주려는 시도
- 필요 이상으로 큰 모달과 비어 있는 여백
- 시험 문제 이동마다 URL 또는 browser history를 추가하는 구조
- 응시 중 정답·배점 외 민감한 채점 정보를 노출하는 UI

## 10. 구현 전 체크리스트

- 이 화면의 사용자가 지금 반드시 해야 하는 행동은 하나인가?
- 강조색은 그 행동과 현재 상태에만 쓰였는가?
- 긴 한국어 텍스트와 유형명이 잘리지 않는가?
- 목록에서 세부 행동을 억지로 모두 보여주고 있지 않은가?
- 위험 행동이 일반 행동과 시각적으로 분리되어 있는가?
- 목록과 상세가 중복 노출되지 않고 Breadcrumb 복귀 경로가 분명한가?
- 응시 화면 변경이 시험 집중, 보안, 뒤로가기 정책을 해치지 않는가?
